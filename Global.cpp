#include "Global.h"

sf::RenderWindow Global::window(sf::VideoMode(800, 600), "GTA V");

float Global::deltaTime = 0;

sf::Color Global::themeColor(50, 50, 50, 255);

sf::Color Global::backGroundColor(100, 100, 100, 255);

sf::Vector2f Global::baseBriqueSize(90, 45);